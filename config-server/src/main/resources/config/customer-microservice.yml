spring:
  mongodb:
    uri: mongodb://root:pass@mongo-db:27017/customer_db?authSource=admin

server:
  port: 8091

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus
  endpoint:
    health:
      show-details: always
